name: numeric_keyboard
publish_to: none
description: Flutter plugin to display a simple numeric keyboard on Android & iOS.
version: 1.0.0
homepage: https://github.com/huextrat/numeric_keyboard

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:

  bouncing_widget:
    git:
      url: https://github.com/jamesnicholls04/bouncing_widget

  flutter:
    sdk: flutter

  

dev_dependencies:
  flutter_test:
    sdk: flutter